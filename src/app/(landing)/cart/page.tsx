import { CartContainer } from '@/components/cart'

export default function CartPage() {
  return (
    <section className="px-4 2xl:px-0 overflow-x-hidden">
      <CartContainer />
    </section>
  )
}
